[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -q --cov=backend --cov=core --cov-report=term-missing
filterwarnings =
    ignore::DeprecationWarning
markers =
    desktop: embedded desktop profile only (excluded from Docker stack verify)

