STREAMCLIP_ENVIRONMENT=development
STREAMCLIP_DATABASE__URL=postgresql+asyncpg://streamclip:streamclip@localhost:5432/streamclip
STREAMCLIP_DATABASE__SYNC_URL=postgresql+psycopg://streamclip:streamclip@localhost:5432/streamclip
STREAMCLIP_REDIS__URL=redis://localhost:6379/0
STREAMCLIP_CELERY__BROKER_URL=redis://localhost:6379/1
STREAMCLIP_CELERY__RESULT_BACKEND=redis://localhost:6379/2
STREAMCLIP_STORAGE__BACKEND=minio
STREAMCLIP_STORAGE__ENDPOINT_URL=http://localhost:9000
STREAMCLIP_STORAGE__PUBLIC_BASE_URL=http://localhost:9000
STREAMCLIP_STORAGE__BUCKET=streamclip
STREAMCLIP_STORAGE__ACCESS_KEY=streamclip
STREAMCLIP_STORAGE__SECRET_KEY=streamclip_secret
# Rate limiting defaults to ON in code — this opt-out is for local dev only.
STREAMCLIP_RATE_LIMIT__ENABLED=false
# Local dev may use a placeholder, but startup logs SECURITY_WARNING.
# REQUIRED outside development: set a non-placeholder value >=32 chars.
# Generate with: openssl rand -hex 32
STREAMCLIP_AUTH__SECRET_KEY=change-me-in-production-use-openssl-rand
STREAMCLIP_AUTH__ALLOW_ANONYMOUS=true
STREAMCLIP_AUTH__DEVICE_SCOPED_ANONYMOUS=true
# STREAMCLIP_WEBHOOKS__ENABLED=true
# STREAMCLIP_WEBHOOKS__URL=https://your-app.com/hooks/streamclip
# STREAMCLIP_WEBHOOKS__SECRET=your-hmac-secret
# Social distribution (OAuth + publish). Required for YouTube/TikTok connect in local Docker.
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Never reuse a local key in production (.env.production.example).
STREAMCLIP_DISTRIBUTION__TOKEN_ENCRYPTION_KEY=
STREAMCLIP_DISTRIBUTION__WEB_ORIGIN=http://localhost:3000
# STREAMCLIP_DISTRIBUTION__YOUTUBE_PUBLISH_ENABLED=true
# STREAMCLIP_DISTRIBUTION__TIKTOK_PUBLISH_ENABLED=false
# Lemon Squeezy commerce (optional — required for LS checkout + remote license activate)
# STREAMCLIP_COMMERCE__LEMON_SQUEEZY_WEBHOOK_SECRET=
# STREAMCLIP_COMMERCE__LEMON_SQUEEZY_API_KEY=
# STREAMCLIP_COMMERCE__LEMON_SQUEEZY_BETA_VARIANT_ID=
# STREAMCLIP_COMMERCE__LEMON_SQUEEZY_PRO_VARIANT_ID=
# STREAMCLIP_COMMERCE__LEMON_SQUEEZY_CHECKOUT_URL=https://<store>.lemonsqueezy.com/checkout/buy/<variant_id>
# Metrics auth (optional) — if set, /metrics requires this as Bearer token or X-Metrics-Key header.
# In non-dev environments without a key, /metrics is restricted to loopback (127.0.0.1).
# Generate with: openssl rand -hex 16
# STREAMCLIP_OBSERVABILITY__METRICS_API_KEY=

# --- Operator notify (optional; see docs/OPS_ALERTING.md) ---
# Distinct from STREAMCLIP_WEBHOOKS__* above (publish events + optional HMAC).
# OPS_WEBHOOK_URL: HTTPS JSON POST for bug_report / beta_feedback / job_failed /
# stack_degraded. No signature header — URL is the secret. Prefer Zapier/Make
# Catch Hook or a custom JSON inbox (native Discord/Slack hooks need an adapter).
# Set on api + worker + beat.
# Mock egress (no secret): .\scripts\verify_ops_webhook.ps1 -DryRun ; .\scripts\verify_ops_webhook.ps1
# Prefer OPS_WEBHOOK_URL. Legacy N8N_OPS_WEBHOOK_URL still read if primary empty (GAP O13).
# OPS_WEBHOOK_URL=https://hooks.example.com/streamclip-ops
#
# SMTP: RECOMMENDED Phase 0 alerting channel (no third-party connector needed).
# Covers bug-report / beta-feedback email, password reset, Lemon Squeezy key
# fallback, and — when OPS_WEBHOOK_URL is unset — job_failed / stack_degraded.
# Delivery runs on the Celery worker: set these on api + worker.
# Verify: .\scripts\verify_smtp_alerting.ps1 -DryRun  then without -DryRun
# Without SMTP_HOST, forms still persist in DB (no notification).
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=streamclip@example.com
# SMTP_STARTTLS=true
# BUG_REPORT_TO=bugs@example.com
# Resend (verify sending domain in Resend dashboard first):
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=587
# SMTP_USER=resend
# SMTP_PASSWORD=<resend_api_key>
# SMTP_FROM=alerts@your-verified-domain.example
# SMTP_STARTTLS=true
# BUG_REPORT_TO=ops@your-domain.example

# Optional Sentry (API + Celery workers) — STREAMCLIP_OBSERVABILITY__SENTRY_DSN=https://...@o....ingest.sentry.io/...

# Desktop installer signing (operator host / CI only — never commit real certs)
# Windows Authenticode (electron-builder CSC_*). See packaging/installer/README.md
# CSC_LINK=C:\secure\streamclip-ev.pfx
# CSC_KEY_PASSWORD=
# STREAMCLIP_REQUIRE_SIGNED_INSTALLER=1
# GitHub Actions secrets: WINDOWS_CSC_LINK, WINDOWS_CSC_KEY_PASSWORD
# macOS Developer ID + notarization — see packaging/installer/MACOS.md
# CSC_NAME=
# APPLE_ID=
# APPLE_APP_SPECIFIC_PASSWORD=
# APPLE_TEAM_ID=
# GitHub Actions secrets: MACOS_CSC_LINK, MACOS_CSC_KEY_PASSWORD, MACOS_CSC_NAME, APPLE_*
